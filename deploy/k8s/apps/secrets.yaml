apiVersion: v1
kind: Secret
metadata:
  name: homeguard-secrets
  namespace: homeguard-apps
type: Opaque
stringData:
  jwt-secret: "${JWT_SECRET}"
  gemini-text-api-key: "${GEMINI_TEXT_API_KEY}"
  gemini-vision-api-key: "${GEMINI_VISION_API_KEY}"
