{{- if .Values.notificationService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.global.namePrefix }}-notification-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "iot-homeguard-apps.labels" . | nindent 4 }}
    app: {{ .Values.global.namePrefix }}-notification-service
spec:
  replicas: {{ .Values.notificationService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.global.namePrefix }}-notification-service
  template:
    metadata:
      labels:
        app: {{ .Values.global.namePrefix }}-notification-service
    spec:
      containers:
        - name: notification-service
          {{- if .Values.imageRegistry.registry }}
          image: "{{ .Values.imageRegistry.registry }}/{{ .Values.imageRegistry.prefix }}-{{ .Values.notificationService.image }}:{{ .Values.notificationService.tag }}"
          {{- else }}
          image: "{{ .Values.imageRegistry.prefix }}-{{ .Values.notificationService.image }}:{{ .Values.notificationService.tag }}"
          {{- end }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.notificationService.port }}
          env:
            - name: PORT
              value: {{ .Values.notificationService.port | quote }}
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.namePrefix }}-apps-config
                  key: REDIS_URL
          resources:
            {{- toYaml .Values.notificationService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.notificationService.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.notificationService.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.namePrefix }}-notification-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "iot-homeguard-apps.labels" . | nindent 4 }}
    app: {{ .Values.global.namePrefix }}-notification-service
spec:
  selector:
    app: {{ .Values.global.namePrefix }}-notification-service
  ports:
    - port: 8080
      targetPort: {{ .Values.notificationService.port }}
      name: http
{{- end }}
